[package]
name = "tests"
version = "0.1.0"
authors = ["Hexilee <hexileee@gmail.com>"]
edition = "2018"

[dev-dependencies]
client = { path = "../client", version = "0.1"}
util = { path = "../util", version = "0.1"}
